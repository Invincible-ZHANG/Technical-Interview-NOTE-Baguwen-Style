## 回溯


## 栈
有效括号： 栈+哈希表， 右括号匹配左括号，成功则弹栈，最后返回空栈则正确。

最小栈：维护两个栈，主栈和最小值栈。两个栈顶值都相等时，双弹。相等最小值也要同步入最小栈。

字符串解码 ： 维护两个栈，一个存数字，一个存string


## 堆

数据流中位数：最大堆+最小堆 最大堆（queMin）比最小堆多一个在总数是奇数的时候，维护这两个队列即可，保证数值差1.

